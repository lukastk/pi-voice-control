<?xml version="1.0" encoding="utf-8"?>
<!--
  Config for VoiceAccessibilityService. We only use it to FILTER KEY EVENTS
  (volume keys) — we do not read window content or react to accessibility
  events. flagRequestFilterKeyEvents + canRequestFilterKeyEvents enable
  onKeyEvent(); canRetrieveWindowContent is false so nothing on screen is read.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/accessibility_description"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRequestFilterKeyEvents"
    android:canRequestFilterKeyEvents="true"
    android:canRetrieveWindowContent="false"
    android:notificationTimeout="0" />
