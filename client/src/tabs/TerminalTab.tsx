export function TerminalTab() {
  return (
    <div className="placeholder">
      <h2>Terminal</h2>
      <p>
        Phase 3 will host <code>@wterm/dom</code> here, attached to{" "}
        <code>tmux -L mysystem</code>. The bottom extra-keys row (ESC / TAB /
        CTRL / ALT / arrows / font) appears on coarse pointers.
      </p>
    </div>
  );
}
