export function SettingsTab() {
  return (
    <div className="placeholder">
      <h2>Settings</h2>
      <p>
        Phase 6 will expose: tmux socket name, default folder, STT/TTS
        provider, voice ID, earcon toggles, polling interval, LiveKit URL/keys.
      </p>
    </div>
  );
}
